import { FlowState } from 'src/lib/rx-flow';

// --------------------------------------------------------------------
// --- STAGE 1 --------------------------------------------------------
// --------------------------------------------------------------------
@FlowState({
  name: 'stage1'
})
export class StageOneState {
  isStarted = false;
  isCompleted = false;
}

// --------------------------------------------------------------------
// --- STAGE 2 --------------------------------------------------------
// --------------------------------------------------------------------
@FlowState({
  name: 'stage2'
})
export class StageTwoState {
  isStarted = false;
}
